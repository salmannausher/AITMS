{
  "build": {
    "cmds": [
      "pnpm install --frozen-lockfile",
      "pnpm --filter @aitms/api db:generate",
      "pnpm --filter @aitms/shared build",
      "pnpm --filter @aitms/api build"
    ]
  },
  "start": {
    "cmd": "node apps/api/dist/main.js"
  }
}
